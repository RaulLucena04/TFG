<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="controller.layout.MainLayoutController"
            stylesheets="../../resources/styles/main.css"
            prefWidth="1920" prefHeight="1080">

    <!-- Barra superior -->
    <top>
        <HBox spacing="20" alignment="CENTER_RIGHT" styleClass="header" prefHeight="60">

            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>

            <Label text="NBA Predictor" styleClass="header-title" HBox.hgrow="ALWAYS"/>

            <Label fx:id="lblUser" text="Usuario" styleClass="header-label"/>

            <Label fx:id="lblPoints" text="Puntos: 1000" styleClass="header-label"/>

            <Button text="Cerrar sesión" onAction="#handleLogout"/>
        </HBox>
    </top>

    <!-- Menú lateral fijo -->
    <left>
        <VBox spacing="10" styleClass="sidebar" prefWidth="220" minWidth="220" maxWidth="220" alignment="TOP_CENTER">

            <padding>
                <Insets top="15" right="15" bottom="15" left="15"/>
            </padding>

            <Button text="🏠 Dashboard" onAction="#loadDashboard" maxWidth="Infinity"/>
            <Button text="📅 Partidos" onAction="#loadMatches" maxWidth="Infinity"/>
            <Button text="📊 Estadísticas" onAction="#loadStatistics" maxWidth="Infinity"/>
            <Button text="💰 Apuestas" onAction="#loadBets" maxWidth="Infinity"/>
            <Button text="🏆 Rankings" onAction="#loadRankings" maxWidth="Infinity"/>
            <Button text="👤 Mi Perfil" onAction="#loadProfile" maxWidth="Infinity"/>

            <Separator/>

            <Button fx:id="btnAdmin" text="⚙ Administración" styleClass="admin-button" onAction="#loadAdmin" visible="false" managed="false" maxWidth="Infinity"/>
        </VBox>
    </left>

    <!-- Contenido principal que crece -->
    <center>
        <StackPane fx:id="contentPane" styleClass="content"
                   VBox.vgrow="ALWAYS" HBox.hgrow="ALWAYS" />
    </center>

</BorderPane>

<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="controller.apuestas.ActiveBetsController"
      spacing="20"
      styleClass="active-bets-container">

    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>

    <!-- Cabecera -->
    <HBox spacing="15" alignment="CENTER_LEFT">
        <Button text="← Volver" onAction="#handleBack"/>
        <Label text="📊 Apuestas Activas" styleClass="page-title"/>
    </HBox>

    <!-- Filtro -->
    <HBox spacing="15" alignment="CENTER_LEFT">
        <Label text="Filtrar:"/>

        <ComboBox fx:id="comboFilter"
                  prefWidth="200"
                  promptText="Selecciona filtro"/>

        <Region HBox.hgrow="ALWAYS"/>

        <Button text="🔄 Actualizar" onAction="#handleRefresh"/>
    </HBox>

    <!-- Contenido -->
    <ScrollPane fitToWidth="true" vbarPolicy="AS_NEEDED">
        <VBox fx:id="betsContainer"
              spacing="15"
              alignment="TOP_CENTER">
            <!-- Las apuestas activas se cargarán dinámicamente aquí -->
        </VBox>
    </ScrollPane>

</VBox>
